/**
 * Entry shim. The real root lives in `src/app/App.tsx`; this keeps the RN
 * convention of an App.tsx at the project root while delegating to the
 * structured app layer.
 *
 * @format
 */
export { default } from '@app/App';
